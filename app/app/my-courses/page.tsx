'use client';import { useEffect,useState } from 'react';import CourseCard from '@/components/CourseCard';export default function MyCoursesPage(){const [courses,setCourses]=useState<any[]>([]);useEffect(()=>{(async()=>{const r=await fetch('/api/my-courses',{cache:'no-store'});if(r.ok)setCourses(await r.json());})()},[]);return(<main className="p-6"><h1 className="title mb-4">Meus cursos</h1><div className="grid-3">{courses.map(c=><CourseCard key={c.id} c={c}/> )}</div></main>)}
