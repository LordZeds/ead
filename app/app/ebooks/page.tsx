'use client';import { useEffect,useState } from 'react';export default function EbooksPage(){const [items,setItems]=useState<any[]>([]);useEffect(()=>{(async()=>{const r=await fetch('/api/ebooks');if(r.ok)setItems(await r.json());})()},[]);return(<main className="p-6"><h1 className="title mb-4">Ebooks</h1><div className="grid-3">{items.map(e=>(<div key={e.id} className="card space-y-2">{e.coverUrl&&(<img src={e.coverUrl} alt={e.title} className="w-full h-40 object-cover rounded"/>)}<h3 className="font-semibold text-lg">{e.title}</h3><div className="flex gap-2 pt-2">{e.fileUrl&&<a href={e.fileUrl} target="_blank" className="btn btn-sm">Abrir</a>}</div></div>))}</div></main>)}
