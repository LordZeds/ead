'use client';import { useEffect,useState } from 'react';export default function AdminEbooks(){const [items,setItems]=useState<any[]>([]);const [title,setTitle]=useState('');const [slug,setSlug]=useState('');async function load(){const r=await fetch('/api/ebooks');if(r.ok)setItems(await r.json());}useEffect(()=>{load();},[]);async function create(){const r=await fetch('/api/ebooks',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({title,slug})});if(r.ok){setTitle('');setSlug('');load();}}return(<main className="p-6 space-y-4"><h1 className="title">Ebooks</h1><div className="card space-y-2 max-w-xl"><input className="input" placeholder="Título" value={title} onChange={e=>setTitle(e.target.value)}/><input className="input" placeholder="Slug" value={slug} onChange={e=>setSlug(e.target.value)}/><button onClick={create} className="btn btn-sm">Criar</button></div><ul className="space-y-2">{items.map(e=>(<li key={e.id} className="card flex items-center justify-between"><div><div className="font-medium">{e.title}</div><div className="text-xs text-muted">{e.slug}</div></div></li>))}</ul></main>)}
