export const runtime = 'nodejs';
import { NextRequest,NextResponse } from 'next/server';import { db } from '@/db/client';import { lessons } from '@/db/schema';import { eq } from 'drizzle-orm';export async function PUT(req:NextRequest,{params}:{params:{lessonId:string}}){const b=await req.json();await db.update(lessons).set({title:b.title,description:b.description??null,videoUrl:b.videoUrl??null,attachmentUrl:b.attachmentUrl??null,durationSec:b.durationSec??null,position:b.position??0}).where(eq(lessons.id,params.lessonId));return NextResponse.json({ok:true});}export async function DELETE(_:NextRequest,{params}:{params:{lessonId:string}}){await db.delete(lessons).where(eq(lessons.id,params.lessonId));return NextResponse.json({ok:true});}
